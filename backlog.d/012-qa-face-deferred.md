# 012 Q&A face

Status: deferred

## Goal

Capture, but do not implement yet, an embedded Q&A face over the generated
glance corpus for later operator-ratified exploration.

## Oracle

- The backlog records the deferred face without adding runtime surface.
- No Q&A route, model call, embedding store, or retrieval API ships before a
  consumer and explicit operator approval exist.
